{
  "name": "musicpoll",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev:front": "pnpm --filter front dev",
    "dev:back": "pnpm --filter back dev:app",
    "dev:workers": "pnpm --filter back dev:workers",
    "format": "prettier --write .",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "migrate:create": "migrate create",
    "migrate:up": "migrate up",
    "migrate:down": "migrate down"
  },
  "devDependencies": {
    "migrix": "^0.1.0",
    "prettier": "^3.5.3"
  }
}
